version: 2

jobs:
  build:
    working_directory: /app
    docker:
      - image: docker:17.05.0-ce-git
    steps:
      - checkout
      - setup_remote_docker
      - run:
          name: build docker container
          command: 'docker build -t "grafana/build-container:v0.1" .' # should be just make and needs rights to our docker repo